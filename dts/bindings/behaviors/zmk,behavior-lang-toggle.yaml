description: Alternating language key behavior

compatible: "zmk,behavior-lang-toggle"

include: zero_param.yaml

properties:
  first-key:
    type: int
    required: true
  second-key:
    type: int
    required: true
